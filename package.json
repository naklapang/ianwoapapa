{
  "name": "dana-app",
  "version": "1.0.0",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'Build completed'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
}
